machine:
  python:
    version: 2.7.11

dependencies:
  override:
     - chmod +x gradlew
  pre:
     - pip install awscli
     - aws configure set default.region us-west-2

test:
  override:
    - ./gradlew clean build

deployment:
  development:
    branch: master